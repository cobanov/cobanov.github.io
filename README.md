# cobanov.github.io

GitHub Pages stub. The main site lives at [cobanov.dev](https://cobanov.dev/).

This root page exists so that project documentation hosted under the
`cobanov.github.io` namespace (e.g. [`/semaclust`](https://cobanov.github.io/semaclust/))
has a sensible landing page if someone visits the bare domain.

- Pages source: this branch (`main`), root
- No build step, single static `index.html`
